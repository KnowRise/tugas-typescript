/**
 * TODO:
 * 1. Buat interface `Logger` dengan method `log(pesan: string): void`.
 * 2. Buat class `ConsoleLogger` yang mengimplementasikan `Logger` (cetak pesan dengan "[CONSOLE] {pesan}").
 * 3. Buat class `FileLogger` yang mengimplementasikan `Logger` (cetak "[FILE] {pesan}").
 * 4. Buat fungsi `proses` yang menerima `Logger` dan memanggil `log("Proses selesai")`.
 * 5. Panggil `proses` dengan instance `ConsoleLogger` dan `FileLogger`.
 */

// Tulis kode di sini