/**
 * TODO:
 * 1. Buat type alias `ID` yang merupakan union dari string dan number.
 * 2. Buat type alias `Mahasiswa` berupa object dengan properti: `nim` (string), `nama` (string), `ipk` (number, opsional).
 * 3. Buat variabel `mhs1` bertipe Mahasiswa dengan data: nim "123", nama "Dewi".
 * 4. Buat variabel `idUser` bertipe ID dengan nilai "U-001".
 */

// Tulis kode di sini