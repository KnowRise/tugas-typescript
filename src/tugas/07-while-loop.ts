/**
 * TODO:
 * 1. Buatlah variabel `counter` dengan nilai awal 1.
 * 2. Gunakan while loop untuk mencetak angka dari 1 sampai 5.
 *    Setiap iterasi, cetak "Iterasi ke-" diikuti nilai counter.
 * 3. Pastikan loop berhenti setelah 5.
 */

let counter: number = 1;

while (counter <= 5) {
    console.log("Iterasi ke-", counter);
    counter++;
}