/**
 * TODO:
 * 1. Buat class `Karyawan` dengan properti `nama` (public) dan `gaji` (protected).
 *    Constructor menerima kedua properti.
 *    Method `bekerja()` mencetak "{nama} sedang bekerja."
 * 2. Buat class `Manager` yang mewarisi `Karyawan`.
 *    Tambahkan properti `departemen` (public).
 *    Constructor memanggil super() dan mengisi departemen.
 *    Override method `bekerja()` mencetak "{nama} sedang meeting."
 * 3. Buat instance `Manager` dan panggil method `bekerja()`.
 */

// Tulis kode di sini