/**
 * TODO:
 * 1. Buat class `RekeningBank` dengan:
 *    - `namaPemilik` (public, string)
 *    - `saldo` (private, number)
 *    - `nomorRekening` (readonly, string)
 * 2. Constructor menerima ketiganya.
 * 3. Buat method `tambahSaldo(jumlah: number): void` yang menambah saldo jika jumlah > 0.
 * 4. Buat method `getSaldo(): number` untuk mengambil saldo.
 * 5. Coba ubah `nomorRekening` di luar class (harus error, jadikan komentar).
 */

// Tulis kode di sini