/**
 * TODO:
 * 1. Buat interface `Kendaraan` dengan properti: `merk` (string), `roda` (number), dan method `klakson()` yang void.
 * 2. Buat objek `mobil` yang mengimplementasikan interface tersebut. `klakson` mencetak "Beep!".
 * 3. Buat interface `KendaraanListrik` yang mewarisi `Kendaraan` dan menambah properti `dayaBaterai` (number).
 * 4. Buat objek `motorListrik` dengan type `KendaraanListrik`.
 */

// Tulis kode di sini