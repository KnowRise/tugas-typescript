/**
 * TODO:
 * 1. Buat abstract class `Bentuk` dengan:
 *    - Property `nama` (string, protected)
 *    - Constructor menerima nama.
 *    - Method abstract `hitungLuas(): number`.
 *    - Method konkret `info(): string` mengembalikan "Bentuk: {nama}".
 * 2. Buat class `Lingkaran` yang extends `Bentuk` dengan property `jariJari` (private).
 *    Constructor menerima nama dan jariJari, panggil super.
 *    Implementasi `hitungLuas()` mengembalikan 3.14 * jariJari * jariJari.
 * 3. Buat instance `Lingkaran` dan panggil `info()` dan `hitungLuas()`.
 */

// Tulis kode di sini