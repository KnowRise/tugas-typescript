/**
 * TODO:
 * 1. Buatlah variabel dengan nama `namaDepan` bertipe string dan berisi "Budi".
 * 2. Buatlah variabel dengan nama `usia` bertipe number dan berisi 25.
 * 3. Buatlah variabel dengan nama `isMahasiswa` bertipe boolean dan berisi true.
 * 4. Buatlah variabel dengan nama `nilai` bertipe number tanpa nilai awal (gunakan union type `number | undefined`).
 * 5. Tampilkan semua variabel tersebut dengan console.log.
 */

const namaDepan: string = "Budi";
const usia: number = 25;
const isMahasiswa: boolean = true;
let nilai: number | undefined;

console.log(namaDepan, usia, isMahasiswa, nilai);

